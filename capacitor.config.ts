import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'transfer-app-ui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
